import StartDroch from "./StartDroch.jsx";
import Chat from "./Chat.jsx";
import { useSelector } from "react-redux";
import "./bodyCss/body.css";

const Body = () => {
	// const { bodyState } = useSelector((state) => state.reducers.changeBody);

	// return (
	// 	<div className='body'>
	// 		{bodyState === "mainPage" ? <StartDroch /> : <Chat />}
	// 	</div>
	// );
};

export default Body;
